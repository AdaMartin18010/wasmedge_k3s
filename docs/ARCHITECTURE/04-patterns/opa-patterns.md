# OPA 策略治理模式

## 📑 目录

- [📑 目录](#-目录)
- [1. 概述](#1-概述)
- [2. 核心模式](#2-核心模式)
  - [2.1 策略即代码模式（Policy as Code）](#21-策略即代码模式policy-as-code)
  - [2.2 分层策略模式（Layered Policy）](#22-分层策略模式layered-policy)
  - [2.3 统一决策点模式（Unified Decision Point）](#23-统一决策点模式unified-decision-point)
- [3. 技术实现](#3-技术实现)
  - [3.1 Service Mesh 集成](#31-service-mesh-集成)
  - [3.2 Kubernetes Admission Control](#32-kubernetes-admission-control)
  - [3.3 容器沙盒策略](#33-容器沙盒策略)
- [4. 模式对比矩阵](#4-模式对比矩阵)
- [5. 安全模式](#5-安全模式)
  - [5.1 能力闭包模式（Capability Closure）](#51-能力闭包模式capability-closure)
  - [5.2 最小权限模式（Least Privilege）](#52-最小权限模式least-privilege)
  - [5.3 可证明安全模式（Provable Security）](#53-可证明安全模式provable-security)
- [6. 组合模式](#6-组合模式)
  - [6.1 OPA + Service Mesh](#61-opa--service-mesh)
  - [6.2 OPA + NSM](#62-opa--nsm)
  - [6.3 OPA + GitOps](#63-opa--gitops)
- [7. 演进路径](#7-演进路径)
  - [第一阶段：分散策略（2010-2015）](#第一阶段分散策略2010-2015)
  - [第二阶段：策略引擎（2015-2020）](#第二阶段策略引擎2015-2020)
  - [第三阶段：策略即代码（2020-2025）](#第三阶段策略即代码2020-2025)
  - [第四阶段：可证明安全（2025-）](#第四阶段可证明安全2025-)
- [8. 最佳实践](#8-最佳实践)
  - [8.1 策略分层](#81-策略分层)
  - [8.2 策略测试](#82-策略测试)
  - [8.3 策略版本化](#83-策略版本化)
  - [8.4 决策审计](#84-决策审计)
- [9. 参考资源](#9-参考资源)
  - [学术资源](#学术资源)

---

## 1. 概述

OPA (Open Policy Agent) 策略治理模式提供了一种"策略即代码"的治理范式。它将安全策
略从业务代码中剥离，通过 Rego 语言定义策略，实现统一决策、版本化治理、可证明安全
性。

## 2. 核心模式

### 2.1 策略即代码模式（Policy as Code）

**模式描述**：

- 使用 Rego 语言定义策略规则
- 策略与代码一起版本化管理
- 策略可测试、可审计、可验证

**架构图**：

```text
┌─────────────────────────────────────┐
│      OPA Control Plane              │
│  ├─ Policy Bundles (Git)            │
│  ├─ Decision Logs                   │
│  └─ Discovery Service               │
└─────────────────────────────────────┘
                 ▲
┌─────────────────────────────────────┐
│      OPA Agents (PDP)               │
│  ├─ Sidecar (Istio/Linkerd)         │
│  ├─ Admission Controller (K8s)       │
│  ├─ API Gateway                     │
│  └─ Application Service             │
└─────────────────────────────────────┘
                 ▲
┌─────────────────────────────────────┐
│      Application Layer              │
│  (业务服务、API、数据访问)            │
└─────────────────────────────────────┘
```

### 2.2 分层策略模式（Layered Policy）

**模式描述**：

- 不同层级有不同的策略关注点
- 底层策略：系统调用、资源限制
- 中层策略：网络访问、服务间通信
- 高层策略：业务规则、数据访问

**策略层次**：

```text
┌─────────────────────────────────────┐
│      Business Policy                │
│  (业务规则、数据访问策略)             │
├─────────────────────────────────────┤
│      Service Mesh Policy            │
│  (路由、限流、mTLS)                  │
├─────────────────────────────────────┤
│      Container/Sandbox Policy       │
│  (系统调用、资源限制)                 │
├─────────────────────────────────────┤
│      Infrastructure Policy          │
│  (部署、资源配额)                    │
└─────────────────────────────────────┘
```

### 2.3 统一决策点模式（Unified Decision Point）

**模式描述**：

- 所有策略决策都通过 OPA 统一评估
- 决策结果可追溯、可审计
- 策略变更可回滚

**实现示例**：

```rego
# 统一策略包
package policy

# 沙盒层策略
import data.sandbox.policy

# 服务网格层策略
import data.mesh.policy

# 应用层策略
import data.app.policy

# 统一决策
allow = true {
    sandbox.policy.allow
    mesh.policy.allow
    app.policy.allow
}
```

## 3. 技术实现

### 3.1 Service Mesh 集成

**Istio + OPA**：

```yaml
# Istio AuthorizationPolicy 使用 OPA
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: opa-authz
spec:
  action: CUSTOM
  provider:
    name: "opa"
  rules:
    - to:
        - operation:
            paths: ["/api/*"]
```

**OPA 策略**：

```rego
package istio.authz

default allow = false

allow {
    input.attributes.destination.principal == "spiffe://cluster/ns/default/sa/frontend"
    input.attributes.source.principal == "spiffe://cluster/ns/default/sa/backend"
    input.attributes.request.http.method == "GET"
}
```

### 3.2 Kubernetes Admission Control

**Gatekeeper + OPA**：

```yaml
# Gatekeeper ConstraintTemplate
apiVersion: templates.gatekeeper.sh/v1beta1
kind: ConstraintTemplate
metadata:
  name: k8srequiredlabels
spec:
  crd:
    spec:
      names:
        kind: K8sRequiredLabels
  targets:
    - target: admission.k8s.gatekeeper.sh
      rego: |
        package k8srequiredlabels
        violation[{"msg": msg}] {
          not input.review.object.metadata.labels["app"]
          msg := "label 'app' is required"
        }
```

### 3.3 容器沙盒策略

**seccomp + OPA**：

```rego
package sandbox.policy

# 允许的系统调用
allowed_syscalls = {
    "read", "write", "open", "close",
    "socket", "connect", "accept"
}

# 系统调用策略
allow_syscall = true {
    input.syscall in allowed_syscalls
}
```

## 4. 模式对比矩阵

| 模式         | 传统方式         | OPA 模式              |
| ------------ | ---------------- | --------------------- |
| **策略定义** | 配置文件、硬编码 | Rego 语言、策略即代码 |
| **策略管理** | 分散在各处       | 统一 Git 仓库         |
| **策略测试** | 手动测试         | 自动化单元测试        |
| **策略版本** | 无版本管理       | Git 版本管理          |
| **策略审计** | 日志分析         | 决策日志 + 审计       |
| **策略回滚** | 手动回滚         | Git 回滚              |
| **决策延迟** | 不定             | < 5 ms (P99)          |

## 5. 安全模式

### 5.1 能力闭包模式（Capability Closure）

**模式描述**：

- 通过 OPA 定义允许的能力集合
- 运行时验证能力是否在允许集合内
- 实现最小权限原则

**形式化**：

```text
Capability(u) = { c | c ∈ allowed_capabilities AND OPA(image, labels) ⊢ allow(c) }
```

**实现**：

```rego
package sandbox.capabilities

# 允许的能力集合
allowed_capabilities = {
    "NET_BIND_SERVICE",
    "NET_RAW"
}

# 能力验证
allow_capability = true {
    input.capability in allowed_capabilities
    input.image.labels["security-policy"] == "restrictive"
}
```

### 5.2 最小权限模式（Least Privilege）

**模式描述**：

- 默认拒绝所有权限
- 显式授权才允许
- 定期审计权限使用

**实现**：

```rego
package policy.least_privilege

default allow = false

allow {
    # 显式授权规则
    input.user.role == "admin"
    input.resource.owner == input.user.id
}
```

### 5.3 可证明安全模式（Provable Security）

**模式描述**：

- 策略决策可形式化验证
- Rego → AST → SAT 求解
- CI/CD 中自动验证

**实现**：

```bash
# 策略验证
opa eval --format=pretty \
  --data policy.rego \
  --input request.json \
  'data.policy.allow'

# 策略测试
opa test policy.rego
```

## 6. 组合模式

### 6.1 OPA + Service Mesh

**模式**：Adapter 模式

**描述**：

- Service Mesh 作为策略执行点（PEP）
- OPA 作为策略决策点（PDP）
- 通过 ext_authz 集成

**架构图**：

```text
Request ──> Envoy Sidecar ──> OPA Agent ──> Decision
                                      │
                                      └──> Allow/Deny
```

### 6.2 OPA + NSM

**模式**：Bridge 模式

**描述**：

- NSM 使用 OPA 验证 vWire 连接权限
- 跨域访问策略统一管理
- 策略与网络拓扑解耦

**实现**：

```rego
package nsm.vwire

default allow_connection = false

allow_connection {
    input.source.cluster == "cluster-a"
    input.destination.cluster == "cluster-b"
    input.source.labels["env"] == "prod"
    input.destination.labels["env"] == "prod"
}
```

### 6.3 OPA + GitOps

**模式**：Facade 模式

**描述**：

- 策略存储在 Git 仓库
- GitOps 自动同步策略到集群
- 策略变更可追溯、可回滚

**流程**：

```text
Policy.rego (Git) ──> CI/CD ──> OPA Bundle ──> OPA Agent
                                      │
                                      └──> Decision Log (Git)
```

## 7. 演进路径

### 第一阶段：分散策略（2010-2015）

- **特点**：策略分散在配置文件中
- **问题**：难以管理、版本不一致

### 第二阶段：策略引擎（2015-2020）

- **特点**：出现策略引擎（OPA、Kyverno）
- **改进**：策略集中管理、可测试

### 第三阶段：策略即代码（2020-2025）

- **特点**：策略与代码一起版本化管理
- **优势**：可追溯、可回滚、可验证

### 第四阶段：可证明安全（2025-）

- **特点**：策略可形式化验证
- **趋势**：自动验证、自动优化

## 8. 最佳实践

### 8.1 策略分层

- **基础设施层**：资源配额、部署约束
- **运行时层**：系统调用、资源限制
- **网络层**：路由、限流、mTLS
- **应用层**：业务规则、数据访问

### 8.2 策略测试

- **单元测试**：`opa test`
- **集成测试**：CI/CD 中自动测试
- **回归测试**：策略变更自动验证

### 8.3 策略版本化

- **Git 管理**：策略存储在 Git 仓库
- **版本标签**：策略版本与代码版本对齐
- **回滚机制**：策略变更可快速回滚

### 8.4 决策审计

- **决策日志**：记录所有策略决策
- **审计分析**：定期分析决策日志
- **合规报告**：自动生成合规报告

## 9. 参考资源

- **OPA**：<https://www.openpolicyagent.org/>
- **Gatekeeper**：<https://open-policy-agent.github.io/gatekeeper/>
- **Rego 语言**：<https://www.openpolicyagent.org/docs/latest/policy-language/>
- **策略即代码**：<https://www.openpolicyagent.org/docs/latest/policy-as-code/>
- **相关文档**：
  - `architecture-view/08-composition-patterns/04-service-mesh-pattern.md` -
    Service Mesh 模式
  - `04-opa-policy-governance/` - OPA 策略治理详细文档
  - `07-case-studies/financial-system.md` - 金融系统案例（包含 OPA 策略）

### 学术资源

- **[ACADEMIC-REFERENCES.md](../ACADEMIC-REFERENCES.md)** - Wikipedia、大学课程
  、学术论文等学术资源（包含 Open Policy Agent 条目）
- **[REFERENCES.md](../REFERENCES.md)** - 参考标准、框架、工具和资源

---

**更新时间**：2025-11-04 **版本**：v1.0 **参考**：`architecture_view.md` OPA 模
式部分
