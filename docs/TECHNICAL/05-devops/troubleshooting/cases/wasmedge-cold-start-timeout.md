# 案例 W-001：Wasm 应用冷启动超时

> **案例编号**：W-001
> **故障类型**：启动失败
> **严重程度**：严重
> **创建日期**：2025-11-13
> **最后更新**：2025-11-13

---

## 📑 目录

- [案例 W-001：Wasm 应用冷启动超时](#案例-w-001wasm-应用冷启动超时)
  - [📑 目录](#-目录)
  - [1 故障描述](#1-故障描述)
    - [1.1 现象](#11-现象)
    - [1.2 影响范围](#12-影响范围)
    - [1.3 发生时间](#13-发生时间)
  - [2 环境信息](#2-环境信息)
    - [2.1 软件版本](#21-软件版本)
    - [2.2 硬件配置](#22-硬件配置)
    - [2.3 部署配置](#23-部署配置)
  - [3 排查过程](#3-排查过程)
    - [3.1 初步诊断](#31-初步诊断)
    - [3.2 深入分析](#32-深入分析)
    - [3.3 根因定位](#33-根因定位)
  - [4 根因分析](#4-根因分析)
    - [4.1 技术根因](#41-技术根因)
    - [4.2 配置根因](#42-配置根因)
  - [5 解决方案](#5-解决方案)
    - [5.1 临时方案](#51-临时方案)
    - [5.2 根本方案](#52-根本方案)
    - [5.3 预防措施](#53-预防措施)
  - [6 验证结果](#6-验证结果)
    - [6.1 验证方法](#61-验证方法)
    - [6.2 验证结果](#62-验证结果)
    - [6.3 验证时间](#63-验证时间)
  - [7 经验总结](#7-经验总结)
    - [7.1 关键教训](#71-关键教训)
    - [7.2 最佳实践](#72-最佳实践)
    - [7.3 相关文档](#73-相关文档)
  - [8 相关文档](#8-相关文档)

---

## 1 故障描述

### 1.1 现象

**故障现象**：

```bash
$ kubectl get pods
NAME          READY   STATUS    RESTARTS   AGE
hello-wasm    0/1     Pending   0          5m

$ kubectl describe pod hello-wasm
Events:
  Warning  FailedScheduling  5m (x12 over 5m)  default-scheduler
          0/1 nodes are available: 1 Insufficient memory.
  Warning  FailedScheduling  5m (x12 over 5m)  default-scheduler
          0/1 nodes are available: 1 node(s) had taint {node-role.kubernetes.io/master: }.
```

**实际表现**：

- Pod 一直处于 `Pending` 状态
- 调度器报告内存不足
- 应用无法启动，冷启动超时

### 1.2 影响范围

- **受影响服务**：所有使用 Wasm 运行时的应用
- **受影响用户**：所有依赖该服务的用户
- **业务影响**：服务完全不可用，影响生产环境

### 1.3 发生时间

- **首次发现**：2025-11-10 14:30
- **持续时间**：约 2 小时
- **解决时间**：2025-11-10 16:45

---

## 2 环境信息

### 2.1 软件版本

- **K3s 版本**：v1.30.4+k3s1
- **WasmEdge 版本**：v0.14.0
- **crun 版本**：v1.8.5
- **Kubernetes 版本**：v1.30.4
- **操作系统**：Ubuntu 22.04 LTS
- **内核版本**：5.15.0-91-generic

### 2.2 硬件配置

- **节点类型**：边缘节点
- **CPU**：4 核 ARM64
- **内存**：2GB RAM
- **存储**：32GB eMMC

### 2.3 部署配置

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: hello-wasm
  annotations:
    module.wasm.image/variant: compat-smart
spec:
  runtimeClassName: wasm
  containers:
    - name: app
      image: yourhub/hello-wasm:v1
      resources:
        requests:
          memory: "512Mi"  # 问题：请求内存过大
          cpu: "500m"
        limits:
          memory: "1Gi"
          cpu: "1000m"
```

---

## 3 排查过程

### 3.1 初步诊断

**步骤 1：检查 Pod 状态**:

```bash
$ kubectl get pods -o wide
NAME          READY   STATUS    RESTARTS   AGE   IP       NODE
hello-wasm    0/1     Pending   0          5m    <none>   <none>
```

**步骤 2：查看 Pod 事件**:

```bash
$ kubectl describe pod hello-wasm
Events:
  Warning  FailedScheduling  5m (x12 over 5m)  default-scheduler
          0/1 nodes are available: 1 Insufficient memory.
```

**步骤 3：检查节点资源**:

```bash
$ kubectl top nodes
NAME           CPU(cores)   CPU%   MEMORY(bytes)   MEMORY%
edge-node-1   400m         10%    1800Mi         90%
```

**初步结论**：节点内存使用率已达 90%，无法满足 Pod 的 512Mi 内存请求。

### 3.2 深入分析

**步骤 1：检查节点资源详情**:

```bash
$ kubectl describe node edge-node-1
Allocated resources:
  (Total limits may be over 100 percent, i.e., overcommitted.)
  Resource           Requests     Limits
  --------           --------     ------
  cpu                2000m (50%)  4000m (100%)
  memory             1500Mi (75%) 2000Mi (100%)
  ephemeral-storage  0 (0%)       0 (0%)
```

**步骤 2：检查其他 Pod 资源使用**:

```bash
$ kubectl top pods -A
NAMESPACE     NAME                    CPU(cores)   MEMORY(bytes)
kube-system   coredns-xxx             10m          20Mi
kube-system   local-path-provisioner  5m           15Mi
default       other-app-1             50m          100Mi
default       other-app-2             30m          80Mi
```

**步骤 3：分析 Wasm 应用实际内存需求**:

```bash
# 在测试环境中运行 Wasm 应用
$ wasmedge hello-wasm.wasm
# 监控内存使用
$ ps aux | grep wasmedge
USER       PID %CPU %MEM    VSZ   RSS TTY
root      1234  0.1  0.1  2048  2048 ?   # 实际仅使用 2MB
```

**深入分析结论**：

1. 节点内存已分配 1500Mi，剩余可用内存约 500Mi
2. Pod 请求 512Mi，但实际 Wasm 应用仅需约 2-5MB
3. 资源请求配置不合理，导致调度失败

### 3.3 根因定位

**根因 1：资源请求配置过大**:

- Pod 配置中 `resources.requests.memory: "512Mi"` 远大于 Wasm 应用实际需求（2-5MB）
- 导致调度器认为节点资源不足，无法调度 Pod

**根因 2：节点资源紧张**:

- 边缘节点内存仅 2GB，已分配 1500Mi
- 剩余可用内存不足以满足 Pod 的 512Mi 请求

**根因 3：缺乏资源优化**:

- 未根据 Wasm 应用的实际资源需求优化资源配置
- 未考虑边缘节点的资源限制

---

## 4 根因分析

### 4.1 技术根因

**Wasm 应用资源特点**：

1. **内存占用小**：Wasm 应用通常仅需 2-10MB 内存
2. **启动快速**：冷启动时间通常 < 10ms
3. **CPU 占用低**：CPU 使用率通常 < 5%

**配置问题**：

- 使用了容器应用的资源请求标准（512Mi），而非 Wasm 应用的资源需求
- 未考虑边缘节点的资源限制

### 4.2 配置根因

**资源配置不当**：

```yaml
resources:
  requests:
    memory: "512Mi"  # ❌ 过大，Wasm 应用仅需 2-5MB
    cpu: "500m"      # ❌ 过大，Wasm 应用仅需 50-100m
```

**正确的配置**：

```yaml
resources:
  requests:
    memory: "10Mi"   # ✅ 符合 Wasm 应用实际需求
    cpu: "50m"       # ✅ 符合 Wasm 应用实际需求
```

---

## 5 解决方案

### 5.1 临时方案

**方案 1：降低资源请求**:

```bash
# 编辑 Pod 配置
kubectl edit pod hello-wasm

# 修改资源请求
resources:
  requests:
    memory: "10Mi"   # 从 512Mi 降低到 10Mi
    cpu: "50m"       # 从 500m 降低到 50m
```

**方案 2：清理节点资源**:

```bash
# 删除不必要的 Pod
kubectl delete pod other-app-1

# 或调整其他 Pod 的资源限制
kubectl edit pod other-app-1
```

### 5.2 根本方案

**方案 1：优化 Pod 资源配置**:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: hello-wasm
  annotations:
    module.wasm.image/variant: compat-smart
spec:
  runtimeClassName: wasm
  containers:
    - name: app
      image: yourhub/hello-wasm:v1
      resources:
        requests:
          memory: "10Mi"    # ✅ 符合 Wasm 应用实际需求
          cpu: "50m"        # ✅ 符合 Wasm 应用实际需求
        limits:
          memory: "50Mi"    # ✅ 设置合理的上限
          cpu: "200m"       # ✅ 设置合理的上限
```

**方案 2：建立资源基准**:

```bash
# 1. 在测试环境中运行 Wasm 应用
wasmedge hello-wasm.wasm

# 2. 监控实际资源使用
ps aux | grep wasmedge
# 或使用 cgroup 监控
cat /sys/fs/cgroup/memory/memory.usage_in_bytes

# 3. 根据实际使用情况设置资源请求和限制
```

**方案 3：使用资源配额管理**:

```yaml
apiVersion: v1
kind: ResourceQuota
metadata:
  name: wasm-quota
  namespace: default
spec:
  hard:
    requests.memory: "100Mi"  # 限制 Wasm 应用总内存请求
    requests.cpu: "1000m"     # 限制 Wasm 应用总 CPU 请求
    limits.memory: "500Mi"    # 限制 Wasm 应用总内存限制
    limits.cpu: "2000m"       # 限制 Wasm 应用总 CPU 限制
```

### 5.3 预防措施

1. **建立资源基准测试流程**：
   - 在部署前测试 Wasm 应用的实际资源使用
   - 根据测试结果设置合理的资源请求和限制

2. **使用资源模板**：
   - 为 Wasm 应用创建标准的资源模板
   - 避免手动配置导致的错误

3. **监控和告警**：
   - 监控节点资源使用率
   - 当资源使用率超过阈值时发送告警

4. **文档化最佳实践**：
   - 记录 Wasm 应用的典型资源需求
   - 提供资源配置指南

---

## 6 验证结果

### 6.1 验证方法

**步骤 1：应用解决方案**:

```bash
# 更新 Pod 配置
kubectl apply -f hello-wasm-optimized.yaml
```

**步骤 2：检查 Pod 状态**:

```bash
$ kubectl get pods
NAME          READY   STATUS    RESTARTS   AGE
hello-wasm    1/1     Running   0          30s
```

**步骤 3：验证资源使用**:

```bash
$ kubectl top pod hello-wasm
NAME          CPU(cores)   MEMORY(bytes)
hello-wasm    5m           3Mi
```

**步骤 4：验证应用功能**:

```bash
$ kubectl logs hello-wasm
Hello, Wasm!
Application started successfully.
```

### 6.2 验证结果

- ✅ **Pod 状态**：`Running`，应用成功启动
- ✅ **资源使用**：内存 3Mi（远低于请求的 10Mi），CPU 5m（远低于请求的 50m）
- ✅ **应用功能**：应用正常运行，日志输出正常
- ✅ **调度成功**：Pod 成功调度到节点

### 6.3 验证时间

- **验证时间**：2025-11-10 16:45
- **验证人员**：运维团队
- **验证环境**：生产环境

---

## 7 经验总结

### 7.1 关键教训

1. **Wasm 应用资源需求远小于容器应用**：
   - Wasm 应用通常仅需 2-10MB 内存，而非 512MB+
   - 应根据实际需求配置资源，而非使用容器应用的标准配置

2. **边缘节点资源有限**：
   - 边缘节点通常资源有限（2-4GB 内存）
   - 需要优化资源配置，充分利用有限资源

3. **资源基准测试很重要**：
   - 在部署前应测试应用的实际资源使用
   - 根据测试结果设置合理的资源请求和限制

### 7.2 最佳实践

1. **资源请求配置**：
   - Wasm 应用内存请求：5-10Mi（根据实际需求调整）
   - Wasm 应用 CPU 请求：50-100m（根据实际需求调整）

2. **资源限制配置**：
   - Wasm 应用内存限制：20-50Mi（为突发需求留出空间）
   - Wasm 应用 CPU 限制：200-500m（为突发需求留出空间）

3. **资源监控**：
   - 定期监控 Wasm 应用的实际资源使用
   - 根据监控数据优化资源配置

4. **文档化**：
   - 记录 Wasm 应用的典型资源需求
   - 提供资源配置模板和指南

### 7.3 相关文档

- [`../troubleshooting.md`](../troubleshooting.md#1161-启动时间过长) - 故障排查指南
- [`../../../../COGNITIVE/05-decision-analysis/benchmarks/benchmarks.md`](../../../../COGNITIVE/05-decision-analysis/benchmarks/benchmarks.md) - 性能基准文档
- [`../../PRACTICAL-CASE-SUPPLEMENT-PLAN.md`](../../PRACTICAL-CASE-SUPPLEMENT-PLAN.md) - 实践案例补充计划

---

## 8 相关文档

- [`../troubleshooting.md`](../troubleshooting.md) - 故障排查指南
- [`../cases/README.md`](README.md) - 案例集目录
- [`../../../../COGNITIVE/05-decision-analysis/benchmarks/benchmarks.md`](../../../../COGNITIVE/05-decision-analysis/benchmarks/benchmarks.md) - 性能基准文档

---

**最后更新**：2025-11-13
**维护者**：项目团队
**版本**：v1.0
