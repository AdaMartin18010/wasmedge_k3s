# 案例 W-008：Wasm 日志输出异常

> **案例编号**：W-008
> **故障类型**：日志输出故障
> **严重程度**：轻微
> **创建日期**：2025-11-15
> **最后更新**：2025-11-15

---

## 📑 目录

- [案例 W-008：Wasm 日志输出异常](#案例-w-008wasm-日志输出异常)
  - [📑 目录](#-目录)
  - [1. 问题描述](#1-问题描述)
    - [1.1 故障现象](#11-故障现象)
    - [1.2 环境信息](#12-环境信息)
    - [1.3 影响范围](#13-影响范围)
  - [2. 故障排查过程](#2-故障排查过程)
    - [2.1 初步诊断](#21-初步诊断)
    - [2.2 深入排查](#22-深入排查)
    - [2.3 根因分析](#23-根因分析)
  - [3. 解决方案](#3-解决方案)
    - [3.1 临时解决方案](#31-临时解决方案)
    - [3.2 永久解决方案](#32-永久解决方案)
    - [3.3 预防措施](#33-预防措施)
  - [4. 验证与恢复](#4-验证与恢复)
    - [4.1 验证步骤](#41-验证步骤)
    - [4.2 恢复确认](#42-恢复确认)
  - [5. 经验总结](#5-经验总结)
    - [5.1 关键发现](#51-关键发现)
    - [5.2 最佳实践](#52-最佳实践)
    - [5.3 相关文档](#53-相关文档)
  - [6. 相关文档](#6-相关文档)

---

## 1. 问题描述

### 1.1 故障现象

**主要症状**：

- Wasm 应用日志输出异常
- 日志格式不正确，缺少时间戳或日志级别
- 日志输出到错误的位置（stdout 而不是 stderr）
- 日志内容乱码或格式混乱
- 日志丢失，部分日志未输出

**错误日志示例**：

```text
# 正常日志应该如下：
2025-11-15T19:00:00.123Z INFO [wasm-app] Application started
2025-11-15T19:00:00.124Z INFO [wasm-app] Processing request: /api/v1/users

# 实际输出的异常日志：
Application started
Processing request: /api/v1/users
[wasm-app] ERROR: Invalid request
```

**时间线**：

- **19:00:00** - Wasm 应用启动
- **19:00:05** - 开始出现日志输出异常
- **19:00:10** - 日志格式完全混乱
- **19:00:15** - 部分日志丢失

### 1.2 环境信息

**集群信息**：

- **K3s 版本**：v1.30.4+k3s1
- **WasmEdge 版本**：v0.14.0
- **日志收集**：Fluentd + Elasticsearch

**应用配置**：

- **Runtime**：WasmEdge
- **日志级别**：INFO
- **日志格式**：JSON
- **日志输出**：stdout/stderr

**Pod 信息**：

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: wasm-app-log-001
  namespace: default
spec:
  runtimeClassName: wasm
  containers:
    - name: wasm-app
      image: wasm-app:v1.0.0
      env:
        - name: LOG_LEVEL
          value: "INFO"
        - name: LOG_FORMAT
          value: "JSON"
```

### 1.3 影响范围

- **受影响 Pod**：1 个（wasm-app-log-001）
- **受影响服务**：日志收集和监控服务
- **业务影响**：日志无法正常收集和分析，影响问题排查
- **用户影响**：无法通过日志排查问题，影响运维效率

---

## 2. 故障排查过程

### 2.1 初步诊断

**步骤 1：检查 Pod 日志**：

```bash
# 查看 Pod 日志
kubectl logs wasm-app-log-001 -n default

# 输出显示日志格式异常
Application started
Processing request: /api/v1/users
[wasm-app] ERROR: Invalid request
```

**步骤 2：检查日志收集配置**：

```bash
# 检查 Fluentd 配置
kubectl get configmap fluentd-config -n kube-system -o yaml

# 检查日志收集是否正常
```

**步骤 3：检查 WasmEdge 日志配置**：

```bash
# 检查 WasmEdge Runtime 配置
kubectl get runtimeclass wasm -o yaml

# 检查日志输出配置
```

**初步结论**：

- 日志格式不正确，缺少时间戳和日志级别
- 日志输出位置可能不正确
- WasmEdge 日志配置可能有问题

### 2.2 深入排查

**步骤 4：检查应用日志配置**：

```bash
# 检查应用环境变量
kubectl exec -it wasm-app-log-001 -n default -- env | grep LOG

# 输出
LOG_LEVEL=INFO
LOG_FORMAT=JSON
```

**步骤 5：检查 Wasm 模块日志实现**：

```bash
# 检查 Wasm 模块中的日志实现
# 发现日志实现未正确使用 WASI 日志接口
```

**步骤 6：检查日志输出流**：

```bash
# 检查 stdout 和 stderr
kubectl exec -it wasm-app-log-001 -n default -- \
  sh -c "ls -la /proc/1/fd/"

# 发现日志输出到了错误的流
```

**步骤 7：检查日志缓冲**：

```bash
# 检查日志缓冲配置
kubectl describe pod wasm-app-log-001 -n default | grep -i log

# 发现日志缓冲配置不当
```

**深入排查结论**：

- Wasm 模块未正确使用 WASI 日志接口
- 日志输出到了错误的流（stdout 而不是 stderr）
- 日志格式配置未生效
- 日志缓冲导致日志丢失

### 2.3 根因分析

**根因 1：WASI 日志接口使用不当**：

- Wasm 模块未正确使用 WASI 日志接口
- 日志输出未遵循 WASI 标准

**根因 2：日志格式配置未生效**：

- 环境变量配置了日志格式，但应用未读取
- 日志格式化代码存在问题

**根因 3：日志输出流错误**：

- 错误日志输出到了 stdout 而不是 stderr
- 导致日志收集工具无法正确分类

**根因 4：日志缓冲问题**：

- 日志缓冲配置不当，导致日志丢失
- 应用退出时缓冲未刷新

**根本原因**：

**Wasm 模块日志实现不符合 WASI 标准和日志配置未生效**：Wasm 模块未正确使用 WASI 日志接口，且日志格式配置未生效，导致日志输出异常。

---

## 3. 解决方案

### 3.1 临时解决方案

**方案 1：使用日志代理**：

```yaml
# 添加日志代理 Sidecar
apiVersion: v1
kind: Pod
metadata:
  name: wasm-app-log-001
  namespace: default
spec:
  runtimeClassName: wasm
  containers:
    - name: wasm-app
      image: wasm-app:v1.0.0
    - name: log-agent
      image: fluent/fluent-bit:latest
      volumeMounts:
        - name: log-volume
          mountPath: /var/log
```

**方案 2：手动格式化日志**：

```bash
# 使用脚本格式化日志
kubectl logs wasm-app-log-001 -n default | \
  sed 's/^/2025-11-15T19:00:00.123Z INFO [wasm-app] /'
```

**临时方案效果**：

- ✅ 可以快速恢复日志收集
- ⚠️ 但未解决根本问题
- ⚠️ 需要额外的资源消耗

### 3.2 永久解决方案

**方案 1：修复 Wasm 模块日志实现**：

```rust
// 使用 WASI 日志接口
use wasi::logging::logging;

fn log_info(message: &str) {
    logging::log(
        logging::Level::Info,
        "wasm-app",
        message
    ).unwrap();
}

fn log_error(message: &str) {
    logging::log(
        logging::Level::Error,
        "wasm-app",
        message
    ).unwrap();
}

// 使用标准日志格式
fn log_formatted(level: &str, message: &str) {
    let timestamp = chrono::Utc::now().to_rfc3339();
    let log_line = format!("{} {} [wasm-app] {}", timestamp, level, message);

    if level == "ERROR" {
        eprintln!("{}", log_line);  // 错误日志输出到 stderr
    } else {
        println!("{}", log_line);   // 其他日志输出到 stdout
    }
}
```

**方案 2：配置日志格式**：

```rust
// 读取环境变量配置日志格式
use std::env;

fn init_logger() {
    let log_format = env::var("LOG_FORMAT").unwrap_or_else(|_| "TEXT".to_string());
    let log_level = env::var("LOG_LEVEL").unwrap_or_else(|_| "INFO".to_string());

    match log_format.as_str() {
        "JSON" => init_json_logger(&log_level),
        "TEXT" => init_text_logger(&log_level),
        _ => init_text_logger(&log_level),
    }
}

fn init_json_logger(level: &str) {
    // 初始化 JSON 格式日志
    // 输出格式：{"timestamp":"2025-11-15T19:00:00.123Z","level":"INFO","message":"..."}
}

fn init_text_logger(level: &str) {
    // 初始化文本格式日志
    // 输出格式：2025-11-15T19:00:00.123Z INFO [wasm-app] ...
}
```

**方案 3：配置日志输出流**：

```yaml
# 配置日志输出到正确的流
apiVersion: v1
kind: Pod
metadata:
  name: wasm-app-log-001
  namespace: default
spec:
  runtimeClassName: wasm
  containers:
    - name: wasm-app
      image: wasm-app:v1.0.0
      env:
        - name: LOG_STDOUT
          value: "INFO,WARN"  # INFO 和 WARN 输出到 stdout
        - name: LOG_STDERR
          value: "ERROR,FATAL"  # ERROR 和 FATAL 输出到 stderr
```

**方案 4：配置日志缓冲**：

```rust
// 配置日志缓冲和刷新
use std::io::{self, Write};

struct BufferedLogger {
    buffer: Vec<u8>,
    flush_interval: Duration,
}

impl BufferedLogger {
    fn flush(&mut self) -> io::Result<()> {
        io::stdout().write_all(&self.buffer)?;
        io::stdout().flush()?;
        self.buffer.clear();
        Ok(())
    }

    fn log(&mut self, message: &str) {
        self.buffer.extend_from_slice(message.as_bytes());
        self.buffer.push(b'\n');

        // 定期刷新缓冲
        if self.buffer.len() > 4096 {
            self.flush().unwrap();
        }
    }
}

// 应用退出时刷新缓冲
impl Drop for BufferedLogger {
    fn drop(&mut self) {
        self.flush().unwrap();
    }
}
```

### 3.3 预防措施

1. **日志标准**：
   - 遵循 WASI 日志接口标准
   - 使用统一的日志格式
   - 正确使用 stdout 和 stderr

2. **日志配置**：
   - 通过环境变量配置日志格式和级别
   - 支持多种日志格式（JSON、TEXT）
   - 支持日志级别过滤

3. **日志缓冲**：
   - 合理配置日志缓冲大小
   - 定期刷新日志缓冲
   - 应用退出时确保缓冲刷新

4. **日志测试**：
   - 测试日志输出格式
   - 验证日志收集功能
   - 定期检查日志质量

---

## 4. 验证与恢复

### 4.1 验证步骤

**步骤 1：验证日志格式**：

```bash
# 查看 Pod 日志
kubectl logs wasm-app-log-001 -n default

# 应该看到正确格式的日志
2025-11-15T19:00:00.123Z INFO [wasm-app] Application started
2025-11-15T19:00:00.124Z INFO [wasm-app] Processing request: /api/v1/users
```

**步骤 2：验证日志收集**：

```bash
# 检查 Fluentd 日志收集
kubectl logs -n kube-system -l app=fluentd | grep wasm-app

# 应该看到日志被正确收集
```

**步骤 3：验证日志级别**：

```bash
# 测试不同日志级别
kubectl exec -it wasm-app-log-001 -n default -- \
  wasmedge --env LOG_LEVEL=DEBUG wasm-app.wasm

# 应该看到不同级别的日志
```

### 4.2 恢复确认

**恢复时间线**：

- **故障发现**：19:00:00
- **开始排查**：19:00:05
- **根因确认**：19:10:00
- **问题解决**：19:15:00
- **服务恢复**：19:15:05
- **总耗时**：15 分钟

**恢复验证**：

- ✅ 日志格式正确
- ✅ 日志输出到正确的流
- ✅ 日志收集正常
- ✅ 日志质量恢复

---

## 5. 经验总结

### 5.1 关键发现

1. **WASI 日志接口很重要**：
   - 应该使用 WASI 标准日志接口
   - 遵循 WASI 日志规范

2. **日志格式统一**：
   - 使用统一的日志格式
   - 支持多种日志格式（JSON、TEXT）

3. **日志流正确使用**：
   - 错误日志输出到 stderr
   - 普通日志输出到 stdout

4. **日志缓冲管理**：
   - 合理配置日志缓冲
   - 确保缓冲及时刷新

### 5.2 最佳实践

1. **日志标准**：
   - 遵循 WASI 日志接口标准
   - 使用统一的日志格式
   - 正确使用 stdout 和 stderr

2. **日志配置**：
   - 通过环境变量配置日志
   - 支持日志格式和级别配置
   - 支持日志过滤

3. **日志缓冲**：
   - 合理配置缓冲大小
   - 定期刷新缓冲
   - 应用退出时刷新

4. **日志测试**：
   - 测试日志输出
   - 验证日志收集
   - 定期检查日志质量

### 5.3 相关文档

- [`../../TECHNICAL/01-core-foundations/wasmedge/wasmedge.md`](../../TECHNICAL/01-core-foundations/wasmedge/wasmedge.md) - WasmEdge 文档
- [WASI 日志接口规范](https://github.com/WebAssembly/WASI/blob/main/phases/snapshot/docs.md#logging) - WASI 日志标准
- [`../troubleshooting.md`](../troubleshooting.md) - 故障排查指南

---

## 6. 相关文档

- [`../README.md`](README.md) - 故障排查案例集目录
- [`../../TECHNICAL/01-core-foundations/wasmedge/wasmedge.md`](../../TECHNICAL/01-core-foundations/wasmedge/wasmedge.md) - WasmEdge 文档
- [`../troubleshooting.md`](../troubleshooting.md) - 故障排查指南

---

**最后更新**：2025-11-15
**维护者**：项目团队
**版本**：v1.0
