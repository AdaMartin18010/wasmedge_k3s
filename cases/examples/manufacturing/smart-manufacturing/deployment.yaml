# 智能制造 Kubernetes Deployment 配置
# 用途：部署 IoT 数据处理服务到边缘 K3s 集群
# 创建日期：2025-11-15

apiVersion: apps/v1
kind: Deployment
metadata:
  name: iot-data-processor
  namespace: default
  labels:
    app: iot-data-processor
    version: v1.0.0
spec:
  replicas: 100
  selector:
    matchLabels:
      app: iot-data-processor
  template:
    metadata:
      labels:
        app: iot-data-processor
        version: v1.0.0
    spec:
      # 使用 WasmEdge RuntimeClass
      runtimeClassName: wasmedge
      nodeSelector:
        node-type: edge
      containers:
      - name: iot-data-processor
        image: registry.example.com/iot-data-processor:latest
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        env:
        - name: LOG_LEVEL
          value: "info"
        - name: PORT
          value: "8080"
        - name: MQTT_ENABLED
          value: "true"
        ports:
        - containerPort: 1883
          name: mqtt
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
