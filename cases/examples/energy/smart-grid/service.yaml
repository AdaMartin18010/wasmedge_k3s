# 智能电网 Kubernetes Service 配置
# 用途：暴露电力数据处理服务
# 创建日期：2025-11-15

apiVersion: v1
kind: Service
metadata:
  name: power-data-processor
  namespace: default
  labels:
    app: power-data-processor
spec:
  type: ClusterIP
  ports:
  - port: 1883
    targetPort: 1883
    protocol: TCP
    name: mqtt
  selector:
    app: power-data-processor
