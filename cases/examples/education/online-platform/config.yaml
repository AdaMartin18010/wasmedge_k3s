# 在线教育平台配置
# 用途：定义在线教育平台配置参数
# 创建日期：2025-11-15

apiVersion: v1
kind: ConfigMap
metadata:
  name: education-platform-config
  namespace: default
data:
  log_level: "info"
  port: "8080"
  websocket_port: "8081"
  websocket_enabled: "true"
  max_concurrent_students: "1000"
  cache_enabled: "true"
