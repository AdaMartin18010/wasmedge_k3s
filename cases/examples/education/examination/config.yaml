# 考试系统配置
# 用途：定义考试系统配置参数
# 创建日期：2025-11-15

apiVersion: v1
kind: ConfigMap
metadata:
  name: examination-config
  namespace: default
data:
  log_level: "info"
  port: "8080"
  anti_cheat_enabled: "true"
  model_path: "/models/anti-cheat-model.wasm"
  cache_enabled: "true"
  cache_ttl: "300s"
