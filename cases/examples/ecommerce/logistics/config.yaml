# 物流系统配置
# 用途：定义物流系统配置参数
# 创建日期：2025-11-15

apiVersion: v1
kind: ConfigMap
metadata:
  name: logistics-config
  namespace: default
data:
  log_level: "info"
  port: "8080"
  iot_device_enabled: "true"
  tracking_update_interval: "30s"
  cache_enabled: "true"
