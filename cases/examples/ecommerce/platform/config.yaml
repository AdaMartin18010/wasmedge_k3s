# 电商平台配置
# 用途：定义电商平台配置参数
# 创建日期：2025-11-15

apiVersion: v1
kind: ConfigMap
metadata:
  name: ecommerce-platform-config
  namespace: default
data:
  log_level: "info"
  port: "8080"
  max_orders_per_user: "100"
  cache_enabled: "true"
  cache_ttl: "300s"
