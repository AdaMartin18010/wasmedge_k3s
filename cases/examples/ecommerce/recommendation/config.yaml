# 推荐系统配置
# 用途：定义推荐系统配置参数
# 创建日期：2025-11-15

apiVersion: v1
kind: ConfigMap
metadata:
  name: recommendation-config
  namespace: default
data:
  log_level: "info"
  port: "8080"
  model_path: "/models/recommendation-model.wasm"
  cache_enabled: "true"
  cache_ttl: "300s"
  max_recommendations: "20"
