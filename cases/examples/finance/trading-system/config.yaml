# 交易系统配置
# 用途：定义交易系统配置参数
# 创建日期：2025-11-15

apiVersion: v1
kind: ConfigMap
metadata:
  name: trading-config
  namespace: default
data:
  log_level: "info"
  port: "8080"
  low_latency_mode: "true"
  cpu_affinity: "true"
  network_optimization: "true"
  cache_enabled: "true"
  cache_ttl: "10s"
