# 风控系统配置
# 用途：定义风控系统配置参数
# 创建日期：2025-11-15

apiVersion: v1
kind: ConfigMap
metadata:
  name: risk-control-config
  namespace: default
data:
  log_level: "info"
  port: "8080"
  model_path: "/models/risk-model.wasm"
  risk_threshold: "0.8"
  cache_enabled: "true"
  cache_ttl: "300s"
