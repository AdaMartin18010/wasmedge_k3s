# 智能物流配置
# 用途：定义智能物流配置参数
# 创建日期：2025-11-15

apiVersion: v1
kind: ConfigMap
metadata:
  name: logistics-config
  namespace: default
data:
  log_level: "info"
  port: "8080"
  cache_enabled: "true"
  cache_ttl: "10s"
  max_connections: "20000"
  query_timeout: "20ms"
